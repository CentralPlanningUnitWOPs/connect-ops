# Connect Ops

Bespoke outreach packs, one folder per DSP owner, each on an unguessable slug.

Every page ships `noindex` plus a robots disallow: link-shareable, never searchable.
These readers are competitors with one another, so folder names are never sequential
or predictable.
